services:
  mllm_service:
    container_name: govision_mllm
    image: govision_mllm_service:v1.0
    ports:
      - "50051:50051"
    networks:
      - caddy_network

networks:
  caddy_network:
    external: true